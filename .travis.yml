os: linux
dist: xenial

language: node_js
node_js:
  - "10"
cache: npm

install: true

script:
  - travis_wait npm ci
  - lerna run build > /dev/null 2>&1
  - npm test -- --runInBand --ci --bail

before_deploy:
  - echo "//registry.npmjs.org/:_authToken=\${NPM_TOKEN}" >> $HOME/.npmrc 2> /dev/null

deploy:
  - provider: script
    skip_cleanup: false
    script: npm run lerna:publish-next
    on:
      branch: develop
  - provider: script
    skip_cleanup: false
    script: npm run lerna:publish
    on:
      tags: true
